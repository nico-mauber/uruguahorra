{
  "framework": null,
  "buildCommand": "npm install --legacy-peer-deps --force && npx @expo/cli@latest export --platform web --output-dir dist",
  "outputDirectory": "dist",
  "installCommand": "npm install --legacy-peer-deps --force"
}