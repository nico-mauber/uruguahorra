{
  "framework": null,
  "buildCommand": "npm install --legacy-peer-deps --no-package-lock && node build-web.js",
  "outputDirectory": "dist",
  "installCommand": "npm install --legacy-peer-deps --no-package-lock",
  "env": {
    "NPM_CONFIG_CACHE": "/tmp/.npm",
    "NODE_ENV": "production"
  }
}